"""
Cost Melt - Security Integration Reference

The authentication, rate-limiting, and CORS wiring shown here previously
now lives for real in main.py (APIKeyManager, AuthMiddleware,
RateLimitMiddleware, and the /auth router are all registered there).

See main.py for the current, tested integration. This file is kept only
as a pointer so old bookmarks/links don't 404; it intentionally has no
executable code left to drift out of sync with main.py.
"""
